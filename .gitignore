__pycache__/
*build*/
# ...but the ATI `builder/` source package is tracked (the *build*/ rule above is
# for CMake/out-of-source build dirs, not this Python package).
!python/template_instantiation/builder/
*.swp
tritonsrc/tune-*.json
*.csv
*.png
1
2
1.*
2.*
*.out
*.err
*.log
*.cfg
agent-plans

# Python packaging artifacts
*.egg-info/

# Retired legacy generator (kept on disk for reference, not tracked; see
# agent-plans/ati_retire_v3python.md). The live generator is the `aotriton`
# package under python/.
v3python.retiring/

# Session review artifacts (not repo content)
ati-code-quality.md
ati-ccreview-*.md
agent-files/
