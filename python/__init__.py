# Copyright © 2026 Advanced Micro Devices, Inc.
# SPDX-License-Identifier: MIT

"""
The `aotriton` code-generation package.

This directory (`python/`) is mapped to the importable top-level package
`aotriton` by the repo-root `setup.py` (`pip install -e python` / `pip install .`).
Sub-directories here become `aotriton.<subpkg>` (e.g. `python/codegen/` ->
`aotriton.codegen`). The code generator + ATI front-end are relocated here from
`v3python/` during the modular migration (agent-plans/ati_modular_exec0.md).
"""
