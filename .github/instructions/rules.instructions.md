---
applyTo: "v3python/rules/**/*.py"
---

The current usage of `PERF_CHOICES` member is only to provide a default value,
and specify data type for the related fields in generated code. The actual
value supplied can be any value that fall into the datatype specified in
`PERF_CHOICES`.

