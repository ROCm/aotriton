#!/bin/bash
# Placeholder: the Triton-mainline tester build flow is being reworked to use
# the git mirror caching in common-git-cache.sh.
echo ".ci/triton-tester-build.sh is under redesign" >&2
exit 1
